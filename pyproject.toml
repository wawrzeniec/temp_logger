[project]
name = "temperature-logger"
version = "0.1.0"
description = "Temperature logger system with SQLite backend"
requires-python = ">=3.9"
dependencies = [
    "flask>=3.0.0",
    "numpy>=2.0.2",
    "pyserial>=3.5",
    "requests>=2.32.5",
]

[dependency-groups]
dev = [
    "ruff>=0.1.0",
]
